//module aliases 
var Engine = Matter.Engine,
    // Render = Matter.Render,
    World = Matter.World,
    Bodies = Matter.Bodies

function setup() {
    createCanvas(600, 600)
}

function draw() {
    background(51)
}